<script setup>
import { onMounted} from 'vue';
import { useAuthStore } from '@/stores/auth';

const authStore = useAuthStore();

onMounted(async () => {
    await authStore.getProfile();
    console.log(authStore.user);
})


</script>

<template>
    <!-- header -->
    <div class="flex justify-between items-center border-b-1 border-gray-200 pb-2">
        <p class="text-gray-500">សូមស្វាគមន៍ក្នុងការត្រឡប់មកវិញ </p>
        <div class="flex items-center font-fira">
            <div class="w-[30px] h-[30px] rounded-full bg-gray-400 mr-2 overflow-hidden">
                <img src="" alt="" class="w-full h-full object-cover">
            </div>
            <span class="text-blue-900 font-medium">
                {{ authStore.user?.name }}
            </span>
        </div>
    </div>
    <!-- header -->
</template>